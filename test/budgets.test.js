// Tests for the budgets routes

const assert = require('assert')
const db = require('../models')

// beforeEach(async (done) => {
//     await db.User.clear()
//     const johnDoe = new db.User({
//         name: 'John Doe',
//         email: 'john@email.com',
//         password: 'john1234'
//     })
//     johnDoe.save(done)
// })

// describe('POST route for budgets/:id', () => {
//     it('should create a new budget for a user', (done) => {
//         const 
//     })
// })